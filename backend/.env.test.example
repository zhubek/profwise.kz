# Database Configuration (Test Environment)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/profwise_test?schema=public&connection_limit=95&pool_timeout=30&connect_timeout=10"

# PostgreSQL Credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=profwise_test

# JWT Configuration
JWT_SECRET=your-test-jwt-secret-change-this-in-production
JWT_EXPIRES_IN=7d

# Email Configuration (Brevo)
BREVO_API_KEY=your-brevo-api-key
ENABLE_EMAIL_VERIFICATION=false
EMAIL_FROM=test@profwise.kz
EMAIL_FROM_NAME=Profwise Test
FRONTEND_URL=https://test.profwise.kz

# Redis Configuration
ENABLE_REDIS_CACHE=true
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=
REDIS_DEFAULT_TTL=3600

# Application Configuration
PORT=4001
NODE_ENV=test

# CORS Origins (already configured in main.ts)
# Test domains: https://test.profwise.kz, https://test-api.profwise.kz
